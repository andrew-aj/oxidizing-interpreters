#[derive(Eq, PartialEq, Debug, Clone, Copy)]
pub enum OpCode {
    Constant(usize),
    Nil,
    True,
    False,
    Pop,
    GetLocal(usize),
    SetLocal(usize),
    GetGlobal(usize),
    DefineGlobal(usize),
    SetGlobal(usize),
    GetUpvalue(usize),
    SetUpvalue(usize),
    GetProperty(usize),
    SetProperty(usize),
    GetSuper(usize),
    Equal,
    Greater,
    Less,
    Add,
    Subtract,
    Multiply,
    Divide,
    Not,
    Negate,
    Print,
    Jump,
    JumpIfFalse,
    Loop,
    Call(usize),
    Invoke(usize),
    SuperInvoke(usize),
    Closure(usize),
    CloseUpvalue,
    Return,
    Class(usize),
    Inherit,
    Method(usize),
}
